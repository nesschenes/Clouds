namespace Clouds.Enums
{
    /// <summary>Scene value will affect active scene priority</summary>
    public enum Scenes
    {
        Initialize,
        Game,
        GameUI,
        LoadUI,
    }
}